"""Figure modules shipped with the package.

These render the published Hurricane Milton comparisons and the QuerySet
diagnostic maps. Importing this package pulls in no plotting backend; each
module imports matplotlib itself.
"""
